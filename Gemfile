# install with 'bundle install'
# might have to install nokogiri manually (adjust version):
# sudo env ARCHFLAGS="-arch x86_64" gem install nokogiri:1.6.8.1 -- --use-system-libraries --with-xml=/usr/local/Cellar/libxml2/
source 'https://rubygems.org'
plugins:
 - 'github-pages'
 - 'guard'
 - 'guard-jekyll-plus'
 - 'guard-livereload'
